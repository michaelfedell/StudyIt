const React = require("react-native");

const { StyleSheet } = React;

export default {
  container: {
    backgroundColor: "#FFF"
  },
  question: {
    padding: 0,
    margin: 0,
    color: "blue"
  }
};
